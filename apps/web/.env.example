NEXT_PUBLIC_API_URL=<api_url>
# Sunucu bileşenleri (Docker içi SSR): tarayıcı localhost kullanır, Node api servisine gider.
# API_URL=http://api:3001
# JWT_SECRET proje kökündeki .env(.example) ile verilir (hem api hem web aynı değer).
